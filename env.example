# ===========================================
# BACKEND ENVIRONMENT VARIABLES
# ===========================================

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=your_database_name
MONGODB_COLLECTION=your_collection_name

# JWT Authentication
SECRET_KEY=your_super_secret_jwt_key_here_make_it_long_and_random
DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=admin123

# Cloudinary Configuration (for file storage)
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# Qdrant Vector Database Configuration
QDRANT_URL=https://your-qdrant-instance.com
QDRANT_API_KEY=your_qdrant_api_key
QDRANT_COLLECTION_NAME=learn_vector

# NVIDIA AI Configuration
NVIDIA_BASE_URL=https://api.nvidia.com/v1
NVIDIA_API_KEY=your_nvidia_api_key
NVIDIA_CHAT_MODEL=nvidia/llama2-70b
NVIDIA_EMBEDDING_MODEL=nvidia/embed-qa-v1

# ===========================================
# FRONTEND ENVIRONMENT VARIABLES
# ===========================================

# Next.js Public API URL (accessible in browser)
NEXT_PUBLIC_API_URL=http://localhost:8000

# ===========================================
# DEPLOYMENT NOTES
# ===========================================
# 
# For Vercel deployment:
# 1. Set all these variables in Vercel dashboard under Settings > Environment Variables
# 2. Make sure NEXT_PUBLIC_API_URL points to your deployed backend URL
# 3. Ensure all backend services (MongoDB, Qdrant, Cloudinary, NVIDIA) are accessible from Vercel
# 4. Test each service connection before deployment
#
# For local development:
# 1. Copy this file to .env.local
# 2. Update values with your actual credentials
# 3. Never commit .env.local to version control
